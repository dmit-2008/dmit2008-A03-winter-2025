// create a file called Title.js
// create a component and default export it.
// that has one prop called text
export default function Title({text}) {
  // i'm going to return jsx here
  return <h1>{text}</h1>
}
// import and use it in your index.js